# ========================================
# InstaBot - Environment Configuration
# ========================================
# Copy this file to .env in the same directory and adjust values
# DO NOT commit the .env file to version control!

# --- PostgreSQL Database Configuration ---
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=instagram

# --- JWT Authentication Configuration ---
CODE_ALGORITHM=HS256
ACCESS_TOKEN_SECRET=change_me_access_token_secret_generate_strong_random_value
REFRESH_TOKEN_SECRET=change_me_refresh_token_secret_generate_strong_random_value
ACCESS_TOKEN_EXP=600
REFRESH_TOKEN_EXP=30

# --- Instagram Webhook Configuration ---
VERIFY_TOKEN=change_me_instagram_webhook_verification_token

# --- OpenRouter AI API Configuration ---
OPENROUTER_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL_KEY=google/gemini-2.5-flash
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_IMAGE_MODEL_KEY=google/gemini-2.5-flash-image-preview
OPENROUTER_TIMEOUT=30
OPENROUTER_MAX_TOKENS=1500
OPENROUTER_TEMPERATURE=0.7

# --- MinIO Object Storage Configuration ---
MINIO_HOST=minio
MINIO_PORT=9000
MINIO_USER=admin
MINIO_PASSWORD=password123
MINIO_BUCKET=images
MINIO_PUBLIC_URL=http://localhost:9000

# ========================================
# Production Checklist:
# ========================================
# ✓ Generate strong random secrets for ACCESS_TOKEN_SECRET and REFRESH_TOKEN_SECRET
#   Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
# ✓ Set secure passwords for DB_PASSWORD and MINIO_PASSWORD
# ✓ Configure MINIO_PUBLIC_URL to be publicly accessible
# ✓ Obtain and configure your OpenRouter API key from https://openrouter.ai/
# ✓ Set VERIFY_TOKEN for Instagram webhook security
# ✓ Use environment-specific values (dev/staging/production)
# ✓ Never commit .env file to version control
# ✓ Use secrets management in production (e.g., AWS Secrets Manager)
